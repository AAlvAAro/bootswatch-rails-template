%legend.center
  Editar Usuario

.col-md-4.col-md-offset-4
  = form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f|
    = general_error_message(resource)

    .well
      %fieldset.form-group
        .col-md-12
          = f.label :email
          = f.email_field :email, autofocus: true, autocomplete: "email", class: field_with_validation(resource, :email)
          = field_error_message(resource, :email)
        - if devise_mapping.confirmable? && resource.pending_reconfirmation?
          %div
            Esperando confirmación para el correo electrónico: #{resource.unconfirmed_email}
        .col-md-12
          = f.label :password
          %br
          %i (dejar en blanco si no quieres cambiar tu contraseña)
          = f.password_field :password, autocomplete: "off", class: field_with_validation(resource, :password)
          = field_error_message(resource, :password)
          - if @minimum_password_length
            %em
              minimo
              = @minimum_password_length
              caracteres
        .col-md-12
          = f.label :password_confirmation
          = f.password_field :password_confirmation, autocomplete: "off", class: field_with_validation(resource, :password_confirmation)
          = field_error_message(resource, :password_confirmation)
        .col-md-12
          = f.label :current_password
          %br
          %i (debes ingresar tu contraseña actual para confirmar tus cambios)
          = f.password_field :current_password, autocomplete: "off", class: field_with_validation(resource, :current_password)
          = field_error_message(resource, :current_password)
        .col-md-12
          %br
          = f.submit "Actualizar", class: 'btn btn-success'
    = link_to "Atras", :back

  .row.center
    %hr
    %h4 Cancelar mi cuenta
    %p
      ¿Ya no necesitas tu cuenta?
      #{button_to "Cancelar mi cuenta", registration_path(resource_name), data: { confirm: "¿Estás seguro?" }, method: :delete, class: 'btn btn-sm btn-danger' }
